import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Samhati — Free AI for Everyone, Forever",
  description: "Decentralized AI inference network. Every user is a node. Like BitTorrent for intelligence — nobody pays because everyone shares. Built on Solana, powered by TOPLOC proofs.",
  keywords: ["AI", "decentralized", "inference", "Solana", "TOPLOC", "open source", "P2P"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
