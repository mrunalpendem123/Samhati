[package]
name = "cluster-manager"
version = "0.1.0"
edition = "2021"

[dependencies]
bincode = { version = "2.0.0-rc.3", features = ["serde"] }
proximity-router = { path = "../proximity-router" }
serde_bytes = "0.11.19"
